<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
       android:shape="oval">
    <!-- NOTE: Cannot use attributes in drawables :( -->
    <size
        android:width="@dimen/shutter_size"
        android:height="@dimen/shutter_size"/>
    <solid
        android:color="@color/primary_grey_300"/>
    <stroke
        android:color="@color/primary_grey_700"
        android:width="@dimen/shutter_border_width"/>
</shape>