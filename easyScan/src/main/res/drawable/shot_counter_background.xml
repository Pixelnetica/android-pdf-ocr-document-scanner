<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
       android:shape="oval">
    <!-- NOTE: Cannot use attributes in drawables :( -->
    <size
        android:width="@dimen/shot_counter_size"
        android:height="@dimen/shot_counter_size"/>
    <solid
        android:color="@color/accent_indigo_500"/>
    <stroke
        android:color="@color/primary_grey_700"
        android:width="@dimen/shot_counter_border_width"/>
</shape>